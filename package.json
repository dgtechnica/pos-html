{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "npx @tailwindcss/cli -i ./dist/src/main.css -o ./dist/style.css --watch --minify"
    },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.3",
    "tailwindcss": "^4.0.3"
  }
}
